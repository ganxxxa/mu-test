import { createTheme } from "@mui/material";

const theme = createTheme({
  spacing: 4,

  palette: {
    background: {
      default: "#f7f7fd",
      paper: "#f7f7fd",
    },
  },
});

export default theme;
